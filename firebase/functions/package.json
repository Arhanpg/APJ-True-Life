{
  "name": "apj-true-life-functions",
  "description": "APJ TRUE LIFE — Firebase Functions (NOT deployed — Spark plan compatible)",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {},
  "private": true,
  "scripts": {
    "deploy": "echo 'Do NOT deploy functions. Custom claims handled by api-gateway. See README.' && exit 1"
  }
}
